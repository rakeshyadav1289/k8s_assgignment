apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flask-express-ingress
spec:
  rules:
  - host: app.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 8000
